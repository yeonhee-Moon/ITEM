<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.item.join.mapper.JoinMapper">

    <update id="reset" parameterType="java.util.Map">
        UPDATE user
        SET PW = #{password}
        WHERE USER_ID = #{userid}
    </update>

</mapper>