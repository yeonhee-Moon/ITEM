<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.item.confirm.mapper.ConfirmMapper">
	
	<insert id="insertConfirm" parameterType="com.example.item.confirm.model.Confirm">
       	INSERT INTO confirmlist (ID , DESCRIPT, IMAGE)
        VALUES (#{id}, #{descript}, #{imageData})
    </insert>

	<update id="updateConfirm" parameterType="com.example.item.confirm.model.Confirm">
        UPDATE confirmlist 
        SET IMAGE = #{imageData}, DESCRIPT = #{descript} 
        WHERE ID = #{id}
    </update>

    <select id="getImageInfoById" parameterType="Long" resultType="com.example.item.confirm.model.ImageInfo">
        SELECT IMAGE, DESCRIPT
        FROM confirmlist
        WHERE ID = #{id}
    </select>
</mapper>